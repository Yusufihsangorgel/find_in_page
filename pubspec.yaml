name: find_in_page
description: >-
  Ctrl+F for Flutter: a find-in-page bar that highlights every match across your
  widgets, jumps between them, and scrolls the active match into view.
version: 0.1.3
repository: https://github.com/Yusufihsangorgel/find_in_page
issue_tracker: https://github.com/Yusufihsangorgel/find_in_page/issues

topics:
  - search
  - find
  - highlight
  - text
  - widgets

screenshots:
  - description: >-
      Typing a query highlights every match on the page; next/previous
      jump between matches and scroll the active one into view.
    path: doc/demo.gif

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
